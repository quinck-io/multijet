{
    "name": "@libs/prisma-utils",
    "private": true,
    "version": "0.0.1",
    "types": "src/index.ts",
    "main": "src/index.ts",
    "scripts": {
        "typecheck": "tsc",
        "clean": "rm -rf .turbo node_modules",
        "prisma": "prisma",
        "prisma:generate": "prisma generate"
    },
    "devDependencies": {
        "prisma": "^4.11.0"
    },
    "dependencies": {
        "@prisma/client": "^4.11.0"
    }
}
