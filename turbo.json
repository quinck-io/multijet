{
    "pipeline": {
        "typecheck": {},
        "build": {
            "dependsOn": ["^build", "typecheck"],
            "outputs": ["dist/**", ".next/**"]
        },
        "lint": {},
        "openapi:generate": {}
    },
    "globalDependencies": ["tsconfig.base.json"]
}
